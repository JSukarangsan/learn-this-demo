# Insights

Read on top of `../CLAUDE.md`.

**Read `definitions.md` before answering anything with a number in it.** Most wrong
answers about this product are not analysis errors. They're someone using a real
definition that's real for a different question.

- `definitions.md` — what our metrics mean, which source wins, and what a move signals.

## What isn't here

The warehouse. Schemas, tables, and lineage are in dbt and the catalog, and both are
better at describing themselves than this folder would be. What's here is the part
the warehouse cannot tell you: which of several correct definitions is the one we use,
and what a number moving actually means.
